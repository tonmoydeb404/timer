# Appwrite backend (Appwrite Cloud). Get these from the console after
# creating the project (see Phase 0 checklist).
APPWRITE_ENDPOINT=https://fra.cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=
# Server API key with database scopes — ONLY for running the
# provisioning script. Never ship this in any client.
APPWRITE_API_KEY=
