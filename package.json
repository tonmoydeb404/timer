{
  "name": "tymar",
  "version": "0.1.3",
  "private": true,
  "description": "Tymar — Effortless time tracking for focused work.",
  "license": "MIT",
  "homepage": "https://tonmoydeb.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/tonmoydeb404/tymar.git"
  },
  "keywords": [
    "tauri",
    "desktop-app",
    "tymar"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter @apps/web dev",
    "dev:desktop": "pnpm --filter @apps/desktop dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "sync-brand": "node scripts/sync-brand.mjs",
    "sync-brand:check": "node scripts/sync-brand.mjs --check",
    "provision:appwrite": "node scripts/provision-appwrite.mjs",
    "release:tag": "node scripts/release-tag-sync.mjs",
    "prepare": "git config core.hooksPath .husky || true"
  },
  "devDependencies": {
    "dotenv": "^18.0.1",
    "prettier": "^3.7.4",
    "turbo": "^2.10.9",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
