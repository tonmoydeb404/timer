node_modules

.DS_Store

.turbo
.playwright-mcp

# Tauri updater signing keypair — never commit
.keys

# Local env files (Appwrite endpoint/project/API keys) — never commit.
# Only `.env.example` files are tracked.
.env*
!.env.example
