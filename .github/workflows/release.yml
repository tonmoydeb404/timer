name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  release-macos:
    permissions:
      contents: write
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      # App name, slug, and version all come from brand.json / tauri.conf.json,
      # so this workflow needs no per-app edits.
      - name: Read app version and brand
        id: brand
        shell: bash
        run: |
          version=$(node -p "require('./apps/desktop/src-tauri/tauri.conf.json').version")
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "app_name=$(node -p "require('./brand.json').appName")" >> "$GITHUB_OUTPUT"
          echo "slug=$(node -p "require('./brand.json').slug")" >> "$GITHUB_OUTPUT"

      - name: Build and release
        if: github.event_name == 'push'
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: apps/desktop
          tagName: v__VERSION__
          releaseName: "${{ steps.brand.outputs.app_name }} v__VERSION__"
          releaseDraft: false
          prerelease: false
          args: --target aarch64-apple-darwin

      - name: Build only
        if: github.event_name == 'workflow_dispatch'
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: apps/desktop
          args: --target aarch64-apple-darwin

      - name: Upload .dmg (macOS)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.brand.outputs.slug }}-macos-v${{ steps.brand.outputs.version }}.dmg
          path: apps/desktop/src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
          archive: false
          if-no-files-found: error

  release-linux:
    permissions:
      contents: write
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf build-essential

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Read app version and brand
        id: brand
        shell: bash
        run: |
          version=$(node -p "require('./apps/desktop/src-tauri/tauri.conf.json').version")
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "app_name=$(node -p "require('./brand.json').appName")" >> "$GITHUB_OUTPUT"
          echo "slug=$(node -p "require('./brand.json').slug")" >> "$GITHUB_OUTPUT"

      - name: Build and release
        if: github.event_name == 'push'
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: apps/desktop
          tagName: v__VERSION__
          releaseName: "${{ steps.brand.outputs.app_name }} v__VERSION__"
          releaseDraft: false
          prerelease: false
          args: --bundles deb

      - name: Build only
        if: github.event_name == 'workflow_dispatch'
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: apps/desktop
          args: --bundles deb

      - name: Upload .deb (Linux)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.brand.outputs.slug }}-linux-v${{ steps.brand.outputs.version }}.deb
          path: apps/desktop/src-tauri/target/release/bundle/deb/*.deb
          archive: false
          if-no-files-found: error

  release-windows:
    permissions:
      contents: write
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Read app version and brand
        id: brand
        shell: bash
        run: |
          version=$(node -p "require('./apps/desktop/src-tauri/tauri.conf.json').version")
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "app_name=$(node -p "require('./brand.json').appName")" >> "$GITHUB_OUTPUT"
          echo "slug=$(node -p "require('./brand.json').slug")" >> "$GITHUB_OUTPUT"

      - name: Build and release
        if: github.event_name == 'push'
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: apps/desktop
          tagName: v__VERSION__
          releaseName: "${{ steps.brand.outputs.app_name }} v__VERSION__"
          releaseDraft: false
          prerelease: false

      - name: Build only
        if: github.event_name == 'workflow_dispatch'
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: apps/desktop

      - name: Upload .msi (Windows)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.brand.outputs.slug }}-windows-v${{ steps.brand.outputs.version }}.msi
          path: apps/desktop/src-tauri/target/release/bundle/msi/*.msi
          archive: false
          if-no-files-found: error

      - name: Upload .exe (Windows)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.brand.outputs.slug }}-windows-v${{ steps.brand.outputs.version }}.exe
          path: apps/desktop/src-tauri/target/release/bundle/nsis/*.exe
          archive: false
          if-no-files-found: error

  update-homebrew-cask:
    needs: release-macos
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Read app version and brand
        id: brand
        shell: bash
        run: |
          version=$(node -p "require('./apps/desktop/src-tauri/tauri.conf.json').version")
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "app_name=$(node -p "require('./brand.json').appName")" >> "$GITHUB_OUTPUT"
          echo "slug=$(node -p "require('./brand.json').slug")" >> "$GITHUB_OUTPUT"

      - name: Compute DMG sha256
        id: sha
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # release is still a draft, so download via authenticated gh cli instead of the public URL
          gh release download "v${{ steps.brand.outputs.version }}" \
            --repo "${{ github.repository }}" \
            --pattern "${{ steps.brand.outputs.app_name }}_${{ steps.brand.outputs.version }}_aarch64.dmg" \
            --output dmg
          sha256=$(shasum -a 256 dmg | cut -d' ' -f1)
          echo "sha256=$sha256" >> "$GITHUB_OUTPUT"

      - name: Update cask
        run: |
          version="${{ steps.brand.outputs.version }}"
          sha256="${{ steps.sha.outputs.sha256 }}"
          cask="Casks/${{ steps.brand.outputs.slug }}.rb"
          sed -i "s/version \".*\"/version \"$version\"/" "$cask"
          sed -i "s/sha256 .*/sha256 \"$sha256\"/" "$cask"

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet -- "Casks/${{ steps.brand.outputs.slug }}.rb" && exit 0
          git add "Casks/${{ steps.brand.outputs.slug }}.rb"
          git commit -m "chore: update homebrew cask to v${{ steps.brand.outputs.version }}"
          git push origin HEAD:main
